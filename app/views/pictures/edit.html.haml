= form_for @picture, url: user_picture_path(current_user, @picture) do |f|
  %fieldset
    %legend Edit 
    = render partial: 'form', locals: {f: f}

    .form-group.row
      .col-md-offset-2
        .col-md-5
          = f.submit "Save", class: "btn btn-primary"
          = link_to "Cancel", user_pictures_path(current_user), class: "btn btn-link"
    - if f.object.errors.any?
      = render partial: 'shared/errors', locals: {f: f}